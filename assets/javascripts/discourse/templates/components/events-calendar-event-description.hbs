<div class="{{event.classes}} event-description skydive-event-color-{{skydive-calendar-jumpable-color event.title}}" style="{{event.listStyle}}">
    {{skydive-calendar-description-summary event.topic}}
    AA
    {{event.topic.firstPost.raw}}
    xxx
    {{event.topic.first_post.raw }}
    ZZ
</div>

{{#if showEventCard}}
  {{events-calendar-card topic=event.topic selectEvent="selectEvent"}}
{{/if}}
